# Robots.txt pour Club Intelligence Artificielle - Université Laval
#
# Toutes les routes sont publiques et servies par la même SPA, donc un seul
# Allow suffit — l'ancienne liste route par route ne servait qu'à devenir
# fausse (elle annonçait encore /mangaai, /lenia et /decisiontree, supprimés).
# Les répertoires listés en Disallow n'existaient pas non plus dans le build.

User-agent: *
Allow: /

# Éviter d'indexer la même page sous ses variantes de tracking
Disallow: /*?*utm_*
Disallow: /*?*fbclid*

Sitemap: https://cia.ift.ulaval.ca/sitemap.xml

Crawl-delay: 1
