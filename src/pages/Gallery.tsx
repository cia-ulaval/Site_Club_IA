function Gallery() {
  return (
    <section>
      <div className="container mx-auto p-4 text-center">
        <h1 className="text-5xl font-bold gradient-text mb-6">Gallery</h1>
        <div>
          make a grid here with pictures and style it instead of that ugly
          carousel we can't make look good
        </div>
      </div>
    </section>
  );
}

export default Gallery;
